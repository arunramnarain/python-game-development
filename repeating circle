import pgzrun
import random

TITLE="circle"
WIDTH=400
HEIGHT=400
def draw():
    r=255
    g=0
    b=random.randint(0,255)
    ra=100
    for i in range(20):
        screen.draw.circle((200,200),(ra),(r,g,b))
        ra -= 5
        r -= 10
        g += 10

pgzrun.go()
